<div class="max-w-4xl"><h1 class="text-3xl font-semibold">{{ __('ui.audit') }}</h1><div class="mt-8 space-y-4">@forelse($events as $event)<article class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><div class="flex flex-wrap justify-between gap-3"><h2 class="font-semibold">{{ __('ui.actions.'.$event->action) }}</h2><time class="text-sm text-zinc-500 dark:text-zinc-400" dir="ltr">{{ \Carbon\Carbon::parse($event->created_at)->timezone('Africa/Algiers')->format('d/m/Y H:i') }}</time></div><p class="mt-2 text-sm">{{ $event->actor_name??__('ui.system_actor') }} · #{{ $event->subject_id }}</p>@if($event->reason)<p class="mt-2 break-words">{{ $event->reason }}</p>@endif<details class="mt-3"><summary class="cursor-pointer text-sm text-teal-700 dark:text-teal-300">{{ __('ui.details') }}</summary>@foreach(['before','after'] as $side)<h3 class="mt-3 text-sm font-medium">{{ __('ui.'.$side) }}</h3><pre dir="ltr" class="mt-1 max-w-full overflow-auto whitespace-pre-wrap break-all text-xs">{{ json_encode(json_decode($event->$side??'null'), JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre>@endforeach</details></article>@empty<p>{{ __('ui.empty_audit') }}</p>@endforelse{{ $events->links() }}</div></div>
