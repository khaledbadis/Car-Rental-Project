<div class="relative" x-data="{open:false}" @click.outside="open=false" @keydown.escape.window="open=false">
<flux:button icon="bell" variant="ghost" @click="open=!open" x-bind:aria-expanded="open" :aria-label="__('catalog.notifications')" />
@if($alerts->count())<span class="pointer-events-none absolute end-0 top-0 h-2 w-2 rounded-full bg-amber-500" aria-hidden="true"></span>@endif
<div x-show="open" x-cloak class="fixed inset-x-3 top-20 z-40 max-h-96 overflow-auto rounded-xl border border-zinc-200 bg-white p-4 shadow-xl dark:border-zinc-700 dark:bg-zinc-900 sm:absolute sm:inset-x-auto sm:end-0 sm:top-full sm:mt-2 sm:w-80"><h2 class="mb-3 font-semibold">{{ __('catalog.notifications') }} ({{ $alerts->count() }})</h2>@forelse($alerts->take(30) as $alert)<a href="{{ route('vehicles.show',$alert->vehicle_id) }}" class="mb-2 block rounded-lg bg-amber-50 p-3 text-sm text-amber-950 dark:bg-amber-950 dark:text-amber-100">{{ __('catalog.'.$alert->type) }} · #{{ $alert->vehicle_id }}<span class="block">{{ __('catalog.'.$alert->expiry_status) }} · {{ $alert->expires_at->format('d/m/Y') }}</span></a>@empty<p class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('catalog.no_alerts') }}</p>@endforelse</div></div>
