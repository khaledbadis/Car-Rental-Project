<div><h1 class="text-3xl font-semibold">{{ __('ui.staff') }}</h1><div class="mt-8 grid items-start gap-8 xl:grid-cols-2"><section class="space-y-3">@foreach($users as $user)<article wire:key="user-{{ $user->id }}" class="flex flex-wrap items-center justify-between gap-4 rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><div class="min-w-0"><h2 class="break-words font-semibold">{{ $user->name }}</h2><p class="break-all text-sm text-zinc-500 dark:text-zinc-400" dir="ltr">{{ $user->email }}</p><p class="mt-2 text-sm">{{ __('ui.'.$user->role) }} · {{ $user->active?__('ui.active'):__('ui.inactive') }}</p></div><flux:button wire:click="edit({{ $user->id }})">{{ __('ui.edit') }}</flux:button></article>@endforeach{{ $users->links() }}</section>
<form wire:submit="save" class="space-y-5 rounded-2xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><h2 class="text-xl font-semibold">{{ $editing?__('ui.edit'):__('ui.new_staff') }}</h2>
<flux:input wire:model="name" :label="__('ui.name')" /><flux:input wire:model="email" type="email" :label="__('ui.email')" />
<flux:select wire:model="role" :label="__('ui.role')">@foreach(['manager','agent','finance'] as $r)<option value="{{ $r }}">{{ __('ui.'.$r) }}</option>@endforeach</flux:select>
<flux:checkbox wire:model="active" :label="__('ui.active')" />
<flux:input wire:model="password" type="password" :label="__('ui.temporary_password')" autocomplete="new-password" />@if($editing)<p class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('ui.reset_hint') }}</p>@endif
<flux:textarea wire:model="reason" :label="__('ui.reason')" rows="2" />
<div class="flex flex-wrap gap-3"><flux:button type="submit" variant="primary">{{ __('ui.save') }}</flux:button>@if($editing)<flux:button wire:click="clear">{{ __('ui.cancel') }}</flux:button>@endif</div></form></div></div>
