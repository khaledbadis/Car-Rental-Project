<div><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm text-teal-700 dark:text-teal-300">{{ __('ui.workspace') }}</p><h1 class="mt-1 text-3xl font-semibold">{{ __('catalog.'.$kind.'s') }}</h1></div>@can($kind==='vehicle'?'fleet.manage':'customers.manage')<flux:button icon="plus" variant="primary" wire:click="$toggle('creating')">{{ __('catalog.add_'.$kind) }}</flux:button>@endcan</div>
@if($creating)<section class="mt-6 rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900 sm:p-7"><h2 class="mb-5 text-xl font-semibold">{{ __('catalog.add_'.$kind) }}</h2><x-duplicate-warning :duplicates="$duplicates" />
@if($kind==='vehicle')<form wire:submit="addCategory" class="mb-6 flex flex-wrap items-end gap-3"><flux:input wire:model="categoryName" :label="__('catalog.new_category')" /><flux:button type="submit" icon="plus">{{ __('catalog.add_category') }}</flux:button></form>@else<p class="mb-5 text-sm text-zinc-500 dark:text-zinc-400">{{ __('catalog.draft_hint') }}</p>@endif
<form wire:submit="save"><x-catalog-fields :kind="$kind" :categories="$categories" :drivers="$drivers" :customer-type="$form['type']??'individual'" /><div class="mt-6 flex gap-3"><flux:button type="submit" variant="primary" icon="check">{{ __('ui.save') }}</flux:button><flux:button wire:click="$set('creating',false)">{{ __('ui.cancel') }}</flux:button></div></form></section>@endif
<div class="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><div class="min-w-0"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" :placeholder="__('catalog.search')" :aria-label="__('catalog.search')" /></div><flux:select wire:model.live="status" :aria-label="__('catalog.status')"><option value="active">{{ __('catalog.active') }}</option><option value="archived">{{ __('catalog.archived') }}</option><option value="all">{{ __('catalog.all') }}</option></flux:select>@if($kind==='vehicle')<flux:select wire:model.live="category" :aria-label="__('catalog.category_id')"><option value="">{{ __('catalog.all_categories') }}</option>@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</flux:select>@endif</div>
<div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">@forelse($records as $r)<a wire:key="record-{{ $r->id }}" href="{{ route($kind.'s.show',$r->id) }}" class="rounded-2xl border border-zinc-200 bg-white p-6 transition hover:border-teal-500 dark:border-zinc-800 dark:bg-zinc-900"><div class="mb-5 flex items-center justify-between"><flux:icon :name="$kind==='vehicle'?'truck':'user-group'" class="size-7 text-teal-700 dark:text-teal-300"/><span class="text-xs text-zinc-500">#{{ $r->id }}</span></div><h2 class="break-words text-lg font-semibold">{{ $kind==='vehicle'?$r->make.' '.$r->model:$r->name }}</h2><p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">{{ $kind==='vehicle'?$r->registration.' · '.$r->year:__('catalog.'.$r->type) }}</p><p class="mt-4 text-sm">@if($kind==='vehicle')<bdi dir="ltr">{{ number_format((float)$r->daily_rate,str_ends_with($r->daily_rate,'.00')?0:2,',',' ') }} DA</bdi> / {{ __('catalog.day') }}@else<bdi dir="ltr">{{ $r->phone }}</bdi>@endif</p>@if($r->archived_at)<p class="mt-3 text-sm text-amber-700 dark:text-amber-300">{{ __('catalog.archived') }}</p>@endif</a>@empty<div class="col-span-full rounded-2xl border border-dashed border-zinc-300 p-12 text-center dark:border-zinc-700"><flux:icon :name="$kind==='vehicle'?'truck':'user-group'" class="mx-auto mb-4 size-10 text-zinc-400"/><h2 class="font-semibold">{{ __('catalog.no_results') }}</h2><p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">{{ __('catalog.empty_hint') }}</p></div>@endforelse</div><div class="mt-6">{{ $records->links() }}</div></div>
