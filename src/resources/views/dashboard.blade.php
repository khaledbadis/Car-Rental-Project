<x-layouts.app><div class="max-w-5xl"><p class="text-sm font-medium text-teal-700 dark:text-teal-300">{{ __('ui.phase') }}</p><h1 class="mt-3 text-3xl font-semibold tracking-tight sm:text-4xl">{{ __('ui.welcome') }}</h1><p class="mt-4 max-w-xl text-zinc-600 dark:text-zinc-400">{{ __('ui.intro') }}</p>
<div class="mt-10 grid gap-5 sm:grid-cols-2">@foreach(['preferences','staff','settings','audit'] as $page) @if($page==='preferences'||auth()->user()->can(['staff'=>'staff.manage','settings'=>'settings.manage','audit'=>'audit.view'][$page]))<a href="{{ route($page) }}" class="rounded-2xl border border-zinc-200 bg-white p-7 transition hover:border-teal-500 dark:border-zinc-800 dark:bg-zinc-900"><h2 class="text-lg font-semibold">{{ __('ui.'.$page) }} <span aria-hidden="true">↗</span></h2><p class="mt-3 text-sm text-zinc-600 dark:text-zinc-400">{{ __('ui.'.$page.'_hint') }}</p></a>@endif @endforeach</div><p class="mt-8 text-sm text-zinc-500 dark:text-zinc-400">{{ __('ui.later') }}</p></div></x-layouts.app>
